{% extends '::base.html.twig' %}

{% block body -%}
    <h1>tipoCambio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fechaagregado</th>
                <td>{{ entity.fechaAgregado|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Tipocambio</th>
                <td>{{ entity.tipoCambio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tipocambio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tipocambio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('tipocambio_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
