{% extends '::base.html.twig' %}

{% block body -%}
    <h1>iva list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Iva</th>
                <th>Fechaagregado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('iva_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.iva }}</td>
                <td>{% if entity.fechaAgregado %}{{ entity.fechaAgregado|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('iva_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('iva_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('iva_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
