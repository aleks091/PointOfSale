{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DescripcionEspecificacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Especificacionid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('descripcionespecificacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.especificacionId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('descripcionespecificacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('descripcionespecificacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('descripcionespecificacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
